import React from "react";
import FeedBackOptions from "@/components/PostContent/FeedBackOptions";

const FeedbackSection = (params) => {
  return <FeedBackOptions {...params} />;
};

export default FeedbackSection;
